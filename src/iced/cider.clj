(ns iced.cider
  ;;(:require [cider.nrepl :as c.nrepl])
  )

; (def supported-handler
;   (comp
;    c.nrepl/wrap-complete
;    c.nrepl/wrap-debug
;    c.nrepl/wrap-info
;    c.nrepl/wrap-macroexpand
;    c.nrepl/wrap-ns
;    c.nrepl/wrap-out
;    c.nrepl/wrap-pprint
;    c.nrepl/wrap-pprint-fn
;    c.nrepl/wrap-spec
;    c.nrepl/wrap-test
;    c.nrepl/wrap-trace
;    c.nrepl/wrap-undef))
